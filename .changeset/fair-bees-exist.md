---
"opencode-provider-alias": patch
---

Release the provider metadata filtering update so models.dev-only top-level `id`, `env`, and doc metadata are removed while existing user-defined provider config remains intact.
